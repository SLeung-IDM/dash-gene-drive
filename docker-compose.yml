version: "3.9"
services:
  service:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - "DEBUG=1"
    volumes:
      - ./csvs:/app/service/csvs
    container_name: gene_drive
    ports:
      - "8050:8050"
    restart: unless-stopped